# Dependencies
node_modules
**/node_modules

# Build outputs
**/.next
**/dist
**/build
**/.turbo
**/.cache

# Logs
**/*.log

# Env / secrets (never copy into image)
.env
.env.*
!.env.example
!.env.prod.example

# VCS
.git
.gitignore

# OS junk
.DS_Store
Thumbs.db

# Backups (must never go to image)
backup_axion
ori-os
ori-os-chatzai

# npm is the canonical package manager for production images.
